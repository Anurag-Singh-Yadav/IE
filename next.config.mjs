/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["encrypted-tbn0.gstatic.com", "www.shutterstock.com","interview-express.s3.ap-south-1.amazonaws.com"],
  },
};

export default nextConfig;
